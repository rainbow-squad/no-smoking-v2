#  docker compose -f docker-compose.prod-test.yml up --build

services:
  app:
    build:
      context: .
      dockerfile: docker.prod-test.Dockerfile
    env_file:
      - .env.prod
    restart: "no"
    
